# Last Semester
## Countdown + Slider

- Open `index.html` in a browser to view the countdown and auto slider.
- Place your images inside the `images/` folder and list their filenames in `script.js` `images` array, or keep the sample slides provided.

Quick open on Windows (PowerShell):

```powershell
# from project folder
start index.html
```
